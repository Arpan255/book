eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-a:8762/eureka/

server:
  port: 9092
spring:
  application:
    name: book-service
  datasource:
    url: jdbc:mysql://mysqldoc2:3306/books
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true